import { Helmet } from "react-helmet-async";

//for dynamic page title change using react helmet
const Menu = () => {
    return (
        <>
        <Helmet>
            <title>Menu</title>
        </Helmet>
        </>
    );
};

export default Menu;