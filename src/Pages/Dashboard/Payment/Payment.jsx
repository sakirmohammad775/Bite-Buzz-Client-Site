import React from 'react';
import SectionTitle from '../../../Components/SectionTitle/SectionTitle';

const Payment = () => {
    return (
        <div>
            <div>
                <SectionTitle subHeading="Payment" heading="Please pay too eat"></SectionTitle>
            </div>
        </div>
    );
};

export default Payment;