

const SectionTitle = ({heading ,subHeading}) => {
    return (
        <>
        <div>
            <p>{subHeading}</p>
            <p>{heading}</p>
        </div>
        </>
    );
};

export default SectionTitle;